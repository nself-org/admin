// @nself/config — Prettier preset. Matches the ecosystem-wide style.
/** @type {import('prettier').Config} */
export const prettierConfig = {
  semi: false,
  singleQuote: true,
  trailingComma: 'es5',
  tabWidth: 2,
  printWidth: 100,
  arrowParens: 'always',
  endOfLine: 'lf',
}

export default prettierConfig
