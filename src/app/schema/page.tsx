/**
 * /schema — Visual Schema Builder Canvas
 * SP-13.B20
 *
 * Canonical route for the Visual Schema Builder.
 * The full implementation lives at /schema-builder/page.tsx and is
 * re-exported here so both /schema and /schema-builder serve the same page.
 */

export { default } from '@/app/schema-builder/page'
